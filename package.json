{
  "name": "quizfreely",
  "version": "1.0.0",
  "description": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ehanahamed/quizfreely.git"
  },
  "author": "Ehan Ahamed",
  "license": "UPL-1.0",
  "bugs": {
    "url": "https://github.com/ehanahamed/quizfreely/issues"
  },
  "homepage": "https://github.com/ehanahamed/quizfreely#readme",
  "devDependencies": {
    "clean-css": "^5.3.3",
    "foreach-cli": "^1.8.1",
    "html-minifier": "^4.0.0",
    "js-beautify": "^1.14.11",
    "terser": "^5.27.0"
  },
  "scripts": {
    "format": "npx js-beautify --replace '**/*.js' && npx js-beautify --html --replace '**/*.html' && npx js-beautify --css --replace '**/*.css'",
    "build": "rm -rf dist && cp -r src/web dist && npx foreach -g 'dist/**/*.js' -x 'terser #{path} --output #{path}'"
  }
}
