on:
  workflow_dispatch:
jobs:
  format:
    name: Format Source Code
    runs-on: ubuntu-latest
    steps:
    - name: Setup Node.js
      uses: actions/setup-node@v1
    - name: Install Dependencies
      run: npm install
    - name: Run Formatting
      run: npm run format
